map $http_upgrade $connection_upgrade {
  default upgrade;
  '' close;
}

server {
  listen 80;
  server_name _;

  charset utf-8;
  proxy_http_version 1.1;
  proxy_set_header Host $host;
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Proto $scheme;
  proxy_set_header X-Forwarded-Host $host;
  proxy_set_header X-Forwarded-Port $server_port;
  proxy_read_timeout 120s;

  location = /healthz {
    default_type text/plain;
    return 200 "ok\n";
  }

  # Use free-seo-tools-page as the default home UI.
  location = / {
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://free-seo-tools-page:5000/;
  }

  location = /ai-seo-pagescore {
    rewrite ^ /ai-seo-pagescore/ last;
  }

  location ^~ /ai-seo-pagescore/ {
    rewrite ^/ai-seo-pagescore/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://ai-seo-pagescore:5000;
    proxy_redirect ~^(/.*)$ /ai-seo-pagescore$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /ai-seo-pagescore$1;
    proxy_cookie_path / /ai-seo-pagescore/;
  }

  location = /asbestos-atlas {
    rewrite ^ /asbestos-atlas/ last;
  }

  location ^~ /asbestos-atlas/ {
    rewrite ^/asbestos-atlas/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://asbestos-atlas:5000;
    proxy_redirect ~^(/.*)$ /asbestos-atlas$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /asbestos-atlas$1;
    proxy_cookie_path / /asbestos-atlas/;
  }

  location = /btucalc {
    rewrite ^ /btucalc/ last;
  }

  location ^~ /btucalc/ {
    rewrite ^/btucalc/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://btucalc:5000;
    proxy_redirect ~^(/.*)$ /btucalc$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /btucalc$1;
    proxy_cookie_path / /btucalc/;
  }

  location = /dog-bite-map {
    rewrite ^ /dog-bite-map/ last;
  }

  location ^~ /dog-bite-map/ {
    rewrite ^/dog-bite-map/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://dog-bite-map:5000;
    proxy_redirect ~^(/.*)$ /dog-bite-map$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /dog-bite-map$1;
    proxy_cookie_path / /dog-bite-map/;
  }

  location = /free-seo-tools-page {
    rewrite ^ /free-seo-tools-page/ last;
  }

  location ^~ /free-seo-tools-page/ {
    rewrite ^/free-seo-tools-page/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://free-seo-tools-page:5000;
    proxy_redirect ~^(/.*)$ /free-seo-tools-page$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /free-seo-tools-page$1;
    proxy_cookie_path / /free-seo-tools-page/;
  }

  location = /header-hierarchy {
    rewrite ^ /header-hierarchy/ last;
  }

  location ^~ /header-hierarchy/ {
    rewrite ^/header-hierarchy/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://header-hierarchy:5000;
    proxy_redirect ~^(/.*)$ /header-hierarchy$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /header-hierarchy$1;
    proxy_cookie_path / /header-hierarchy/;
  }

  location = /intent-discover {
    rewrite ^ /intent-discover/ last;
  }

  location ^~ /intent-discover/ {
    rewrite ^/intent-discover/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://intent-discover:5000;
    proxy_redirect ~^(/.*)$ /intent-discover$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /intent-discover$1;
    proxy_cookie_path / /intent-discover/;
  }

  location = /keyword-mixer {
    rewrite ^ /keyword-mixer/ last;
  }

  location ^~ /keyword-mixer/ {
    rewrite ^/keyword-mixer/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://keyword-mixer:5000;
    proxy_redirect ~^(/.*)$ /keyword-mixer$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /keyword-mixer$1;
    proxy_cookie_path / /keyword-mixer/;
  }

  location = /legal-lens {
    rewrite ^ /legal-lens/ last;
  }

  location ^~ /legal-lens/ {
    rewrite ^/legal-lens/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://legal-lens:5000;
    proxy_redirect ~^(/.*)$ /legal-lens$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /legal-lens$1;
    proxy_cookie_path / /legal-lens/;
  }

  location = /legal-napaudit {
    rewrite ^ /legal-napaudit/ last;
  }

  location ^~ /legal-napaudit/ {
    rewrite ^/legal-napaudit/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://legal-napaudit:5000;
    proxy_redirect ~^(/.*)$ /legal-napaudit$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /legal-napaudit$1;
    proxy_cookie_path / /legal-napaudit/;
  }

  location = /legal-revamp {
    rewrite ^ /legal-revamp/ last;
  }

  location ^~ /legal-revamp/ {
    rewrite ^/legal-revamp/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://legal-revamp:5000;
    proxy_redirect ~^(/.*)$ /legal-revamp$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /legal-revamp$1;
    proxy_cookie_path / /legal-revamp/;
  }

  location = /legal-schema-gen {
    rewrite ^ /legal-schema-gen/ last;
  }

  location ^~ /legal-schema-gen/ {
    rewrite ^/legal-schema-gen/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://legal-schema-gen:5000;
    proxy_redirect ~^(/.*)$ /legal-schema-gen$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /legal-schema-gen$1;
    proxy_cookie_path / /legal-schema-gen/;
  }

  location = /link-auditor {
    rewrite ^ /link-auditor/ last;
  }

  location ^~ /link-auditor/ {
    rewrite ^/link-auditor/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://link-auditor:5000;
    proxy_redirect ~^(/.*)$ /link-auditor$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /link-auditor$1;
    proxy_cookie_path / /link-auditor/;
  }

  location = /mellino-health-data {
    rewrite ^ /mellino-health-data/ last;
  }

  location ^~ /mellino-health-data/ {
    rewrite ^/mellino-health-data/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://mellino-health-data:3000;
    proxy_redirect ~^(/.*)$ /mellino-health-data$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /mellino-health-data$1;
    proxy_cookie_path / /mellino-health-data/;
  }

  location = /meta-describer {
    rewrite ^ /meta-describer/ last;
  }

  location ^~ /meta-describer/ {
    rewrite ^/meta-describer/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://meta-describer:5000;
    proxy_redirect ~^(/.*)$ /meta-describer$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /meta-describer$1;
    proxy_cookie_path / /meta-describer/;
  }

  location = /ohio-care-finder {
    rewrite ^ /ohio-care-finder/ last;
  }

  location ^~ /ohio-care-finder/ {
    rewrite ^/ohio-care-finder/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://ohio-care-finder:5000;
    proxy_redirect ~^(/.*)$ /ohio-care-finder$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /ohio-care-finder$1;
    proxy_cookie_path / /ohio-care-finder/;
  }

  location = /ohio-maternity-monitor {
    rewrite ^ /ohio-maternity-monitor/ last;
  }

  location ^~ /ohio-maternity-monitor/ {
    rewrite ^/ohio-maternity-monitor/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://ohio-maternity-monitor:5000;
    proxy_redirect ~^(/.*)$ /ohio-maternity-monitor$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /ohio-maternity-monitor$1;
    proxy_cookie_path / /ohio-maternity-monitor/;
  }

  location = /replit-downloader {
    rewrite ^ /replit-downloader/ last;
  }

  location ^~ /replit-downloader/ {
    rewrite ^/replit-downloader/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://replit-downloader:5000;
    proxy_redirect ~^(/.*)$ /replit-downloader$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /replit-downloader$1;
    proxy_cookie_path / /replit-downloader/;
  }

  location = /sales-research-flow {
    rewrite ^ /sales-research-flow/ last;
  }

  location ^~ /sales-research-flow/ {
    rewrite ^/sales-research-flow/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://sales-research-flow:5000;
    proxy_redirect ~^(/.*)$ /sales-research-flow$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /sales-research-flow$1;
    proxy_cookie_path / /sales-research-flow/;
  }

  location = /semrush-scraper {
    rewrite ^ /semrush-scraper/ last;
  }

  location ^~ /semrush-scraper/ {
    rewrite ^/semrush-scraper/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://semrush-scraper:5000;
    proxy_redirect ~^(/.*)$ /semrush-scraper$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /semrush-scraper$1;
    proxy_cookie_path / /semrush-scraper/;
  }

  location = /seo-headline-pro {
    rewrite ^ /seo-headline-pro/ last;
  }

  location ^~ /seo-headline-pro/ {
    rewrite ^/seo-headline-pro/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://seo-headline-pro:5000;
    proxy_redirect ~^(/.*)$ /seo-headline-pro$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /seo-headline-pro$1;
    proxy_cookie_path / /seo-headline-pro/;
  }

  location = /serpsnippet-audit {
    rewrite ^ /serpsnippet-audit/ last;
  }

  location ^~ /serpsnippet-audit/ {
    rewrite ^/serpsnippet-audit/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://serpsnippet-audit:5000;
    proxy_redirect ~^(/.*)$ /serpsnippet-audit$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /serpsnippet-audit$1;
    proxy_cookie_path / /serpsnippet-audit/;
  }

  location = /text-clarity {
    rewrite ^ /text-clarity/ last;
  }

  location ^~ /text-clarity/ {
    rewrite ^/text-clarity/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://text-clarity:5000;
    proxy_redirect ~^(/.*)$ /text-clarity$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /text-clarity$1;
    proxy_cookie_path / /text-clarity/;
  }

  location = /tube-insight {
    rewrite ^ /tube-insight/ last;
  }

  location ^~ /tube-insight/ {
    rewrite ^/tube-insight/(.*)$ /$1 break;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://tube-insight:5000;
    proxy_redirect ~^(/.*)$ /tube-insight$1;
    proxy_redirect ~^https?://[^/]+(/.*)$ /tube-insight$1;
    proxy_cookie_path / /tube-insight/;
  }

  location / {
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_pass http://free-seo-tools-page:5000;
  }
}
